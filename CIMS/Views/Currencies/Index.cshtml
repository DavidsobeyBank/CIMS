@model IEnumerable<CIMS.Models.Currency>

@{
    ViewBag.Title = "Currency Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="d-inline">
    <p class="pull-right d-inline">
        @Html.ActionLink("Create", "Create", null, new { @class = "btn btn-outline-success" })
        @Html.ActionLink("Reactivate", "Activate", null, new { @class = "btn btn-outline-primary" })
    </p>
</div>
<table class="table">
    <tr>
        <th>
            Currency Name
        </th>
        <th>
            Currency Code
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CurrencyName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CurrencyCode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CurrencyID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CurrencyID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CurrencyID })
        </td>
    </tr>
}

</table>
